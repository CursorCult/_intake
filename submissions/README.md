# Submissions

Add one YAML file per proposed project.

- File path: `submissions/<project>.yml`
- Start from: `submissions/_template.yml`

The workflow validates basic schema + required fields. Promotion automation (creating/importing org repos) can be added once governance is ready.

